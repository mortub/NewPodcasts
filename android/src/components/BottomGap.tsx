import React from 'react';
import { Text } from 'react-native';

//for putting a gap at the end of a page,because the track player hides content
const BottomGap =() =>{
    return (
        <Text style={{paddingBottom:500}}></Text>
    )
}

export default BottomGap;